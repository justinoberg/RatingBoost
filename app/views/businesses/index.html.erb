<h1>Your Businesses</h1>

<table class="table table-condensed">
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Phone</th>
      <th>Website</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @businesses.each do |business| %>
      <tr>
        <td><%= link_to business.name, business %></td>
        <td><%= business.address %></td>
        <td><%= business.phone %></td>
        <td><%= business.website %></td>
        <td><%= link_to 'Edit', edit_business_path(business), class: "btn btn-primary" %></td>
        <td><%= link_to 'Destroy', business, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Business', new_business_path, class: "btn btn-primary"%>